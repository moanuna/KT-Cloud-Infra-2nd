apiVersion: v1
kind: Pod
metadata:
  name: multi-container-pod
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
  - name: alpine-sidecar
    image: alpine
    command: ["/bin/sh"]
    args: ["-c", "while true; do echo 'sidecar running...', sleep5; done"]

